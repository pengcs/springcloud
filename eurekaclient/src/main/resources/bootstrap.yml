spring:
  cloud:
    config:
      name: eurekaclient
      label: main
      discovery:
        enabled: true
        serviceId: config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/